server:
  port: 80

---

spring:
  config:
    activate:
      on-profile: local
server:
  port: 8080

---

spring:
  config:
    activate:
      on-profile: dev
server:
  port: 8081

---

spring:
  config:
    activate:
      on-profile: real
server:
  port: 8082

---

property:
  test:
      name: property depth test
propertyTest: test
propertyTestList: a,b,c

---

fruit:
    list:
      - name: banana
        color: yellow
      - name: apple
        color: red
      - name: water melon
        color: green
    color-name : colorName
